{
  "name": "zhivoe-slovo-render-backend",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "postinstall": "python3 -m pip install --target .python-packages -r requirements.txt",
    "start": "PYTHONPATH=.python-packages python3 -m gunicorn app:application --bind 0.0.0.0:${PORT:-3000} --workers 1 --threads 4 --timeout 180"
  }
}
